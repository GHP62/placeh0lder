#ifndef FULL_REMOVE_H
#define FULL_REMOVE_H

#include <stdio.h>
#include <stddef.h>
#include <time.h>
#include <stdlib.h>

//writes random data to each byte in a file 10 times, then delets the file
void fullRemove(char *filePath);

#endif
